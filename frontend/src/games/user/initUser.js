
export function initUser() {
  //for loading data from backend also ?
  let coins = 5
  let pause = false
  let edit = false
  let selected = false
  let upgrades = 0
  return { coins, pause, edit, selected, upgrades }
}